// FlowAI entry point — built step by step. See PROJECT.md for the full plan.
// Step 0 placeholder: confirms the TypeScript toolchain compiles.

export const APP_NAME = "FlowAI";

function main(): void {
  console.log(`${APP_NAME}: scaffold ready.`);
}

main();
